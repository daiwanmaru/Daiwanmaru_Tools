export const metadata = {
    title: 'Markdown Converter - Daiwanmaru Tools',
    description: 'Convert DOCX, PDF, HTML, and TXT into clean, structured Markdown files.',
};

export default function MarkdownConverterLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
