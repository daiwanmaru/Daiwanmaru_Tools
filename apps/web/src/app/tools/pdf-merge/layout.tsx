export const metadata = {
    title: 'Combine to PDF - Daiwanmaru Tools',
    description: 'Merge multiple PDFs and images into a single professional document in seconds.',
};

export default function CombineToPdfLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
